1) The first thing that must be done is set the value of tomcat.home in build.properties file to point to your Tomcat

2) To generate the jar project, deploy and run server, in terminal, run:

ant server.run

Note:  EJB-Example project must be deployed on JBoss 7